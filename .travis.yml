language: go

go:
  - "1.11"
  - master

sudo: true

services:
  - mysql

env:
  global:
    - GO_REPO_FULLNAME="github.com/astaxie/beego"

before_install:
  - service mysql restart
  - mysql -e 'CREATE DATABASE test_db;'

# before_script:
#   - echo "USE mysql;\nUPDATE user SET password=PASSWORD('Cindy1024') WHERE user='root';\nFLUSH PRIVILEGES;\n" | mysql -u root

install:
  - echo "install"

  - go get -v github.com/astaxie/beego
  # - go get github.com/go-sql-driver/mysql
  # - go get github.com/qiuchenlong/quickstart
script:
  - echo "run"
  - sh run.sh
